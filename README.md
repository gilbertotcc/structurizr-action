# Structurizr action

Use Structurizr CLI.

## Inputs

### `args`

**Required** Arguments for Structurizr CLI.

## Outputs

## Example usage

```yaml
uses: gilbertotcc/structurizr-action@...
with:
  args: '-workspace . -format json'
```
